{
  "name": "en10168-schemas",
  "version": "0.1.0",
  "description": "EN10168 JSON schema and dependencies developed by S1Seven",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/s1seven/EN10168-schemas.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/s1seven/EN10168-schemas/issues"
  },
  "homepage": "https://github.com/s1seven/EN10168-schemas#readme",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./*.js",
    "lint:staged": "lint-staged",
    "format": "prettier --write \"**/*.{js,json}\"",
    "test": "jest --runInBand",
    "update-version": "node ./utils/update-version.js",
    "release": "read -p 'GITHUB_TOKEN: ' GITHUB_TOKEN && export GITHUB_TOKEN=$GITHUB_TOKEN && release-it",
    "release:dry-run": "read -p 'GITHUB_TOKEN: ' GITHUB_TOKEN && export GITHUB_TOKEN=$GITHUB_TOKEN && release-it --dry-run",
    "generate-pdf": "node ./utils/generate-pdf-cli"
  },
  "devDependencies": {
    "@markedjs/html-differ": "^3.0.4",
    "@s1seven/schema-tools-generate-html": "^0.0.24",
    "@s1seven/schema-tools-generate-pdf": "^0.0.15",
    "@s1seven/schema-tools-utils": "^0.0.21",
    "ajv": "^8.8.2",
    "ajv-formats": "^2.1.1",
    "eslint": "^8.3.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "glob": "^7.2.0",
    "jest": "^27.4.3",
    "jsdom": "^19.0.0",
    "lint-staged": "^12.1.2",
    "pdf2pic": "^2.1.4",
    "pre-commit": "^1.2.2",
    "prettier": "2.5.0",
    "release-it": "*"
  },
  "lint-staged": {
    "*.{js}": [
      "eslint"
    ]
  },
  "pre-commit": [
    "lint:staged"
  ],
  "release-it": {
    "github": {
      "release": true,
      "requireBranch": "main"
    },
    "npm": {
      "publish": false
    },
    "hooks": {
      "before:init": [
        "npm run lint",
        "npm run test"
      ],
      "after:bump": "npm run update-version v${version} ${name}",
      "after:release": "echo Successfully released ${name} v${version} to ${repo.repository}."
    }
  },
  "jest": {
    "rootDir": ".",
    "maxConcurrency": 2,
    "testRegex": ".*\\.spec\\.js$",
    "moduleFileExtensions": [
      "js",
      "node"
    ]
  }
}
